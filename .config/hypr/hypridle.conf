general {
    lock_cmd = ~/.config/hypr/scripts/lock-session.sh
    before_sleep_cmd = ~/.config/hypr/scripts/lock-session.sh
}

listener {
    timeout = 600
    on-timeout = loginctl lock-session
}

listener {
    timeout = 900
    on-timeout = ~/.config/hypr/scripts/suspend-deep.sh
}
